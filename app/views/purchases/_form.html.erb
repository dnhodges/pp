<%= form_for(@purchase) do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label><%= f.label :pay_type, "Payment:" %></label>
    <%= f.select :pay_type, Purchase::PAYMENT_TYPES, prompt: 'Select a payment method' %>
  </div>

    <br/><br/><br/>
  <div class="field">
    <label> <%= f.label :cc_number, "VISA number (if credit)" %> </label><br/>
    <%= f.text_field :cc_number, prompt: 'If Credit, enter number (VISA only)' %>
  </div>
  <div class="actions">
    <%= f.submit 'Place Order' %>
  </div>
<% end %>
