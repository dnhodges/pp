<%= form_for(@combo) do |f| %>
  <% if @combo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@combo.errors.count, "error") %> prohibited this combo from being saved:</h2>

      <ul>
      <% @combo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= select_tag(:ingredient, options_for_select(@ingredients.collect.each {|ingredient| [ingredient.name, ingredient.id]})) %>

   <%= select_tag(:pizza_size, options_for_select(@pizzas.collect.each {|pizza| pizza.size})) %>

   <%= select_tag(:pizza_crust, options_for_select(@pizzas.collect.each { |pizza| pizza.crust})) %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
